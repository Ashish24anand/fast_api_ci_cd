version: 1.0
runtime: python3

build:
  commands:
    build:
      - pip install --upgrade pip
      - pip install -r requirements.txt

run:
  # 👇 Start your FastAPI app with uvicorn on port 8080
  command: uvicorn main:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
